<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- bo góc -->
    <corners android:radius="50dp" />

    <!-- màu nền -->
    <solid android:color="#F4EEF6" />

    <!-- viền -->
    <stroke
        android:width="1dp"
        android:color="@color/white" />

</shape>
